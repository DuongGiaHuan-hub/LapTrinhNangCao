#include <iostream>
#include <math.h>
#include <cmath>

using namespace std;

int main()
{
    int x;
    cin >> x;
    if(x > 50) cout << "Chon the dang cam di";
    else if(x <= 50) cout << "Chon the con lai";
    return 0;
}
